using UnityEngine;
using System.Collections;

public class BeControlShow : MonoBehaviour
{
    //UILabel label;
	// Use this for initialization
	void Start () {
        //label = transform.GetComponent<UILabel>();
	}
	
	// Update is called once per frame
	void Update () {
        //label.text = "w=" + Screen.width + " h=" + Screen.height;
	}
}
 
